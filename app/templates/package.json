{
  "name": "<%= projectName %>",
  "description": "<%= projectDescription %>",
  "author": "<%= projectAuthor %>",
  "src": "src",
  "test": "test",
  "dist": "dist",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "open http://localhost:21356/webpack-dev-server/ && webpack-dev-server --progress --colors --watch --config ./webpack.config.js",
    "scripts": {
      "serve": "open http://localhost:8080/webpack-dev-server/ && webpack-dev-server --progress --colors --watch",
      "watch": "webpack --progress --colors --watch",
      "build": "webpack --progress --colors",
      "production": "webpack -production --progress --colors",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "history": "2.0.x",
    "mortarjs": "^2.0.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0",
    "react-router": "^2.2.4",
    "wheelbarrow": "*"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^0.1.9",
    "file-loader": "^0.9.0",
    "node-sass": "^3.8.x",
    "sass-loader": "^3.1.2",
    "url-loader": "^0.5.7",
    "webpack": "^1.12.9",
    "webpack-dev-middleware": "^1.4.0",
    "webpack-dev-server": "^1.14.0"
  }
}
